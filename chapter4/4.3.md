# 4.3.1
```
int Index(seqstring S,seqstring T,int pos)
{
    while (i<=S.curlen)&&(j<=T.curlen>)
    if(S.ch[i]==T.ch[j])
    {
        i++;j++;
    }
    else
    {i=i-j+2;j=1;}
    if(j>T.curlen)
    return(i-T.curlen);
    else
    return(0);
}
```

